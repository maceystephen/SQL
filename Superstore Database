#This project analyzes retail inventory data using SQL to answer business questions related to pricing, stock levels, and product performance.

CREATE TABLE superstore (
    item_id INTEGER PRIMARY KEY,
    item_name TEXT,
    category TEXT,
    price DECIMAL(10, 2),
    stock_quantity INTEGER,
    average_rating DECIMAL(3, 2)
);

INSERT INTO superstore (item_id, item_name, category, price, stock_quantity, average_rating)
VALUES
    (1, 'Stainless Steel Cookware Set', 'Kitchen Supplies', 89.99, 50, 4.6),
    (2, 'Memory Foam Mattress', 'Furnishings', 499.99, 30, 4.8),
    (3, 'Smart LED TV', 'Electronics', 549.00, 20, 4.5),
    (4, 'Robot Vacuum Cleaner', 'Appliances', 199.50, 40, 4.3),
    (5, 'Wireless Bluetooth Speaker', 'Electronics', 39.99, 60, 4.2),
    (6, 'Non-Stick Baking Set', 'Kitchen Supplies', 29.95, 80, 4.4),
    (7, 'Cotton Bedding Set', 'Furnishings', 89.00, 25, 4.7),
    (8, 'Smart Home Security Camera', 'Electronics', 79.95, 15, 4.1),
    (9, 'Air Purifier', 'Appliances', 129.50, 35, 4.6),
    (10, 'Premium Coffee Maker', 'Kitchen Supplies', 79.99, 50, 4.9),
    (11, 'Ergonomic Office Chair', 'Furnishings', 189.00, 20, 4.5),
    (12, 'Wireless Earbuds', 'Electronics', 49.99, 75, 4.3),
    (13, 'Slow Cooker', 'Appliances', 49.95, 30, 4.7),
    (14, 'Cutlery Set', 'Kitchen Supplies', 34.50, 40, 4.4),
    (15, 'Cozy Throw Blanket', 'Furnishings', 24.99, 100, 4.2);


#This query identifies high priced products in the inventory. The Smart LED TV and Memory Foam Mattress are among the highest priced items.

SELECT item_name, price
FROM superstore
ORDER BY price desc;

#The following aggregate functions provide an overview of total pricing, average price, and price range across the store’s products.

SELECT SUM(price)
FROM superstore;

SELECT AVG(price)
FROM superstore;

SELECT MIN(price)
FROM superstore;

SELECT MAX(price)
FROM superstore;

SELECT COUNT(price)
FROM superstore;

#This query shows the top priced item in the Kitchen Supplies group, highlighting the highest cost product in that category.

--What is the item that is the highest price in the category 'Kitchen Supplies'?

SELECT item_name, price
FROM superstore
WHERE category = 'Kitchen Supplies'
ORDER BY price desc
LIMIT 1;

#This query evaluates stock availability for a high value electronics product.

--How many Smart LED TV's are in stock?

SELECT item_name, SUM(stock_quantity)
FROM superstore
WHERE item_name = 'Smart LED TV'
GROUP BY item_name;

#This query identifies products that may require restocking to prevent inventory shortages.

--Which product has the lowest stock?

SELECT item_name, stock_quantity
FROM superstore
ORDER BY stock_quantity
LIMIT 1;

#This query highlights high-performing products based on customer satisfaction.

--Which products have a rating above 4.5?

SELECT item_name, average_rating
FROM superstore
WHERE average_rating > 4.5
ORDER BY average_rating DESC;

#This query shows which product category has the highest average price, helping identify where the most expensive products are grouped.

--Which category has the highest average price?

SELECT category, AVG(price)
FROM superstore
GROUP BY category
ORDER BY AVG(price) DESC;
